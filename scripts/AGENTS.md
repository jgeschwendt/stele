# scripts

```stele
kind: container
purpose: Distribution scripts. install.sh/uninstall.sh land or remove the single stele binary; release.sh assembles the per-platform tarball + sha256 the release workflow publishes on a v* tag.
```

<!-- stele:begin router -->
<!-- stele:end -->
