# src

```stele
kind: container
purpose: The stele engine. cli.rs is the command pipeline; model.rs the typed graph, anchors.rs the §2.5 scanner, assert.rs the six §4 checks, extract.rs the extractors, legacy.rs the migrate rewrite.
```

<!-- @stele -->
<!-- @end -->
